/**
 * xKanvas Panel
 * @class Panel Class
 * @extends conect Class
 */
xk.pan = function(o){
    xk.con.call(this,o);
};

xk.extend(xk.pan, xk.con);

xk.pan.prototype = { 
	//width: this.o.width || 578,
    //height: this.o.height || 200
}
